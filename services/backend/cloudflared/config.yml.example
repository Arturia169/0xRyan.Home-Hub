# Cloudflare Tunnel 配置文件示例
# 
# 此文件会在部署时自动生成，这里仅作为参考

# Tunnel ID（从 Cloudflare Dashboard 获取）
tunnel: your-tunnel-id-here

# 凭据文件路径
credentials-file: /etc/cloudflared/credentials.json

# Ingress Rules（转发规则）
ingress:
  # 规则 1：将 app.yukinetwork.eu.org 转发到本地 3000 端口
  - hostname: app.yukinetwork.eu.org
    service: http://localhost:3000
  
  # 默认规则：其他请求返回 404
  - service: http_status:404

# 可选配置
# originRequest:
#   connectTimeout: 30s
#   noTLSVerify: false
